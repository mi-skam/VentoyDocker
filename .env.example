# VentoyDocker Configuration
# Copy this file to .env and customize as needed

# Ventoy version to use
VENTOY_VERSION=1.1.07

# Ventoy download URL (automatically constructed if not specified)
# VENTOY_DOWNLOAD_URL=https://github.com/ventoy/Ventoy/releases/download/v${VENTOY_VERSION}/ventoy-${VENTOY_VERSION}-linux.tar.gz

# Ubuntu base image version
UBUNTU_VERSION=latest

# Docker image and container names
DOCKER_IMAGE_NAME=ventoy-docker
DOCKER_CONTAINER_NAME=ventoy-docker

# Port for Ventoy web interface
VENTOY_WEB_PORT=24680

# NBD (Network Block Device) configuration
NBD_PORT=10809
NBD_DEVICE=/dev/nbd0
NBD_HOST=host.docker.internal

# Ventoy working directory inside container
# VENTOY_WORK_DIR=/root/ventoy-${VENTOY_VERSION}
